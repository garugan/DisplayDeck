# DisplayDeck v0.1.0 — 最終Smoke Test

状態: 未実施。RC未固定。これは記入用手順であり、合格証跡ではない。

## RC・実行記録

- RC名 / Installer絶対path:
- Installer size / SHA256（試験前・試験後）:
- 製品source commit / build証跡:
- Windows edition / build / architecture:
- GPU / driver / 接続Display:
- 実施日時 / operator:
- diagnostic JSON保存先:
- 試験前 / 終了後 / uninstall後のDisplay設定記録:
- 結果 / 問題点:

## 確認項目

- [ ] Windowsへインストールできる
- [ ] 正常起動する
- [ ] 接続Displayが表示される
- [ ] 現在モードが表示される
- [ ] READ_ONLYである
- [ ] Display設定を変更するUIがない/無効
- [ ] diagnostic JSONを取得できる
- [ ] アプリ終了後もWindowsのDisplay設定に変化なし
- [ ] uninstallできる
- [ ] uninstall後もDisplay設定に変化なし

## 実施条件

固定RCだけを試験する。既存インストールのない検証済みWindows 10 exact cellを使い、試験前のDisplay構成・解像度・refresh rateを記録する。設定変更・simulation操作は行わない。失敗、予期しない設定変更、SHA不一致があれば停止して証跡を残し、PASSにしない。試験後もInstallerのSHA256が固定値と同じことを確認する。

Windowsでのinstall / launch / uninstallの正確な操作は、RCの所在確定後に`docs/windows-validation-history.md`へ記録し、commit・pushしてから実施する。590ケース等は再実行しない。

## Gate C

未判定。全項目PASS、artifact同一性、build / installer生成証跡を確認後、human ownerが判定する。Gate Bは前提としない。
